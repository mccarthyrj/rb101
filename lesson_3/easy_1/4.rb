# 4.rb
# What do the following array methods do?

numbers = [1, 2, 3, 4, 5]

p numbers.delete_at(1) # deletes and returns the element at the index passed in as the argument
p numbers

numbers = [1, 2, 3, 4, 5]

p numbers.delete(1) # deletes and returns the explicit element passed in as an arguemnt
p numbers